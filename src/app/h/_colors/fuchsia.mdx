import { color } from '../../globalTokens.stylex'

export const hanky = { name: 'fuschia', value: color.fuschia }
export const flag = {
	title: 'Spanking',
	left: 'spanking',
	right: 'getting spanked',
}
