import { color } from '../../globalTokens.stylex'
import holstein from '../../_patterns/holstein.svg'

export const hanky = { name: 'holstein', pattern: holstein, value: color.holstein }
export const flag = {
	title: 'Milking',
	left: 'milker',
	right: 'gets milked',
}
