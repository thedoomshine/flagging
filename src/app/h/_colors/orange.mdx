import { color } from '../../globalTokens.stylex'

export const hanky = { name: 'orange', value: color.orange }
export const flag = {
	title: 'Anything, anytime',
	left: 'anything, anytime',
	right: 'just crusing (for now)',
}
