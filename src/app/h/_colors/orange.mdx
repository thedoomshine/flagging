import { color } from '../../globalTokens.stylex'
import HoverCard from '../../_components/hover-card'

export const meta = {
	kink: 'Anything, Anytime',
	left: 'anything, anytime',
	right: 'just crusing (for now)',
	name: 'orange',
	hex: color.orange,
}

export const Preview = () => (
	<>
	Orange means you are open to <strong>Anything, Anytime</strong>, and would like people to approach you no matter their desires, you two can figure it out later.
</>
)

<Preview />
