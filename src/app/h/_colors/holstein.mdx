import holstein from '../../_patterns/holstein.svg'
import { color } from '../../globalTokens.stylex'

export const meta = {
	title: 'Milking',
	left: 'milker',
	right: 'gets milked',
	name: 'holstein',
	pattern: holstein,
	value: color.holstein,
}
